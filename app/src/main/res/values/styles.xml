<resources>

  <!-- Application theme -->
  <style name="SplashTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
    <item name="android:windowBackground">@drawable/splash_background</item>
    <item name="android:statusBarColor">@android:color/transparent</item>
    <item name="windowActionBar">false</item>
    <item name="windowNoTitle">true</item>
    <item name="android:windowContentOverlay">@null</item>
    <item name="android:navigationBarColor">?android:attr/colorBackground</item>
  </style>

  <style name="AppTheme" parent="Theme.AppCompat.DayNight.DarkActionBar">
    <item name="android:actionBarSize">36dp</item>
    <item name="actionBarSize">36dp</item>
    <item name="colorPrimary">@color/primary</item> //App bar color
    <item name="colorPrimaryDark">@color/primaryDark</item>  //Status bar color
    <item name="colorAccent">@color/accent</item>
    <item name="colorSecondary">@color/dynamicBg</item>   //Background main activity
    <item name="android:itemBackground">@color/dynamicBg</item>  //Background options menu
    <item name="android:textColor">@color/dynamicFgDim</item>  //Text options menu
    <item name="android:buttonTint">@color/dynamicFgDim</item> //Checkbox color
  </style>

 </resources>
